@page "/fetchbooks"

@using WebUI.Data
@using P7_UrbanRead
@inject MainDataService SearchBook

<PageTitle>Urban Library Search</PageTitle>

<h1>Urban Library</h1>
<div>
    <form id="ULsearch">
        @*<input type="search" id="query" name="q" placeholder="Book...">*@
        <InputText type="search" id="query" name="q" placeholder="Book..." @bind-Value="_searchTerm" />
        <button class="btn btn-primary" @onclick="FetchBook">Search</button>
    </form>
</div>

<div>
    @if (_resultsVisible)
    {
        @if (searchResults == null)
        {
            <p><em>Loading...</em></p>
        }
        else
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>Cover</th>
                        <th>Title</th>
                        <th>Description</th>
                        <th>Authors</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var result in searchResults)
                    {
                        <tr>
                            <td>@result.CoverImgLink</td>
                            <td>@result.Title</td>
                            <td>@result.Description</td>
                            <td>@result.Authors</td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    }
</div>

@code {

    private List<Book> searchResults = new List<Book>();

    private string _searchTerm;
    private bool _resultsVisible = false;

    private void FetchBook()
    {
        _resultsVisible = true;
        searchResults = MainDataService.SearchBook(_searchTerm);
        searchResults.ToString();

        /*
        List<string> searchResultsToString = new List<string>();
        foreach (Book result in searchResults)
        {
            searchResultsToString.Add(result.ToString());
        }
        */

    }

}
